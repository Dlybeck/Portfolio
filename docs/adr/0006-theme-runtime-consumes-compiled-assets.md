# Theme runtime consumes compiled assets

Installed Theme Packs provide complete base and expanded SVG assets for every Board location rather than executable renderers or a runtime SVG composition language. Authoring tools may still compose reusable templates and variation axes, but they compile those sources before installation; this keeps the Theme Engine small, makes human and AI output equally portable, and permits expressive worlds without allowing generated behavior to execute in the portfolio.
