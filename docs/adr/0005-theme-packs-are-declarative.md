# Theme Packs are declarative

Theme Packs are auto-discovered data and sanitized SVG assets interpreted by one Theme Engine; they cannot ship JavaScript or unrestricted CSS. This deliberately trades unlimited per-pack behavior for safe no-code installation, reliable random selection, uniform validation, and a future AI seam that can generate complete visual worlds without modifying or executing application code.
